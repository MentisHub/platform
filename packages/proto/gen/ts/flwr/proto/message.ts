// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.10.1
//   protoc               v3.21.12
// source: flwr/proto/message.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Error } from "./error.js";
import { Node } from "./node.js";
import { RecordDict } from "./recorddict.js";
import { Scalar } from "./transport.js";

export const protobufPackage = "flwr.proto";

export interface Message {
  metadata: Metadata | undefined;
  content: RecordDict | undefined;
  error: Error | undefined;
}

export interface Context {
  runId: string;
  nodeId: string;
  nodeConfig: { [key: string]: Scalar };
  state: RecordDict | undefined;
  runConfig: { [key: string]: Scalar };
}

export interface Context_NodeConfigEntry {
  key: string;
  value: Scalar | undefined;
}

export interface Context_RunConfigEntry {
  key: string;
  value: Scalar | undefined;
}

export interface Metadata {
  runId: string;
  messageId: string;
  srcNodeId: string;
  dstNodeId: string;
  replyToMessageId: string;
  groupId: string;
  ttl: number;
  messageType: string;
  createdAt: number;
}

export interface ObjectIDs {
  objectIds: string[];
}

export interface ObjectTree {
  objectId: string;
  children: ObjectTree[];
}

/** PushObject messages */
export interface PushObjectRequest {
  node: Node | undefined;
  runId: string;
  objectId: string;
  objectContent: Uint8Array;
}

export interface PushObjectResponse {
  stored: boolean;
}

/** PullObject messages */
export interface PullObjectRequest {
  node: Node | undefined;
  runId: string;
  objectId: string;
}

export interface PullObjectResponse {
  objectFound: boolean;
  objectAvailable: boolean;
  objectContent: Uint8Array;
}

/** ConfirmMessageReceived messages */
export interface ConfirmMessageReceivedRequest {
  node: Node | undefined;
  runId: string;
  messageObjectId: string;
}

export interface ConfirmMessageReceivedResponse {
}

function createBaseMessage(): Message {
  return { metadata: undefined, content: undefined, error: undefined };
}

export const Message: MessageFns<Message> = {
  encode(message: Message, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.metadata !== undefined) {
      Metadata.encode(message.metadata, writer.uint32(10).fork()).join();
    }
    if (message.content !== undefined) {
      RecordDict.encode(message.content, writer.uint32(18).fork()).join();
    }
    if (message.error !== undefined) {
      Error.encode(message.error, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Message {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMessage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.metadata = Metadata.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.content = RecordDict.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.error = Error.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Message {
    return {
      metadata: isSet(object.metadata) ? Metadata.fromJSON(object.metadata) : undefined,
      content: isSet(object.content) ? RecordDict.fromJSON(object.content) : undefined,
      error: isSet(object.error) ? Error.fromJSON(object.error) : undefined,
    };
  },

  toJSON(message: Message): unknown {
    const obj: any = {};
    if (message.metadata !== undefined) {
      obj.metadata = Metadata.toJSON(message.metadata);
    }
    if (message.content !== undefined) {
      obj.content = RecordDict.toJSON(message.content);
    }
    if (message.error !== undefined) {
      obj.error = Error.toJSON(message.error);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Message>, I>>(base?: I): Message {
    return Message.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Message>, I>>(object: I): Message {
    const message = createBaseMessage();
    message.metadata = (object.metadata !== undefined && object.metadata !== null)
      ? Metadata.fromPartial(object.metadata)
      : undefined;
    message.content = (object.content !== undefined && object.content !== null)
      ? RecordDict.fromPartial(object.content)
      : undefined;
    message.error = (object.error !== undefined && object.error !== null) ? Error.fromPartial(object.error) : undefined;
    return message;
  },
};

function createBaseContext(): Context {
  return { runId: "0", nodeId: "0", nodeConfig: {}, state: undefined, runConfig: {} };
}

export const Context: MessageFns<Context> = {
  encode(message: Context, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.runId !== "0") {
      writer.uint32(8).uint64(message.runId);
    }
    if (message.nodeId !== "0") {
      writer.uint32(16).uint64(message.nodeId);
    }
    globalThis.Object.entries(message.nodeConfig).forEach(([key, value]: [string, Scalar]) => {
      Context_NodeConfigEntry.encode({ key: key as any, value }, writer.uint32(26).fork()).join();
    });
    if (message.state !== undefined) {
      RecordDict.encode(message.state, writer.uint32(34).fork()).join();
    }
    globalThis.Object.entries(message.runConfig).forEach(([key, value]: [string, Scalar]) => {
      Context_RunConfigEntry.encode({ key: key as any, value }, writer.uint32(42).fork()).join();
    });
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Context {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseContext();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.runId = reader.uint64().toString();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.nodeId = reader.uint64().toString();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          const entry3 = Context_NodeConfigEntry.decode(reader, reader.uint32());
          if (entry3.value !== undefined) {
            message.nodeConfig[entry3.key] = entry3.value;
          }
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.state = RecordDict.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          const entry5 = Context_RunConfigEntry.decode(reader, reader.uint32());
          if (entry5.value !== undefined) {
            message.runConfig[entry5.key] = entry5.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Context {
    return {
      runId: isSet(object.runId) ? globalThis.String(object.runId) : "0",
      nodeId: isSet(object.nodeId) ? globalThis.String(object.nodeId) : "0",
      nodeConfig: isObject(object.nodeConfig)
        ? (globalThis.Object.entries(object.nodeConfig) as [string, any][]).reduce(
          (acc: { [key: string]: Scalar }, [key, value]: [string, any]) => {
            acc[key] = Scalar.fromJSON(value);
            return acc;
          },
          {},
        )
        : {},
      state: isSet(object.state) ? RecordDict.fromJSON(object.state) : undefined,
      runConfig: isObject(object.runConfig)
        ? (globalThis.Object.entries(object.runConfig) as [string, any][]).reduce(
          (acc: { [key: string]: Scalar }, [key, value]: [string, any]) => {
            acc[key] = Scalar.fromJSON(value);
            return acc;
          },
          {},
        )
        : {},
    };
  },

  toJSON(message: Context): unknown {
    const obj: any = {};
    if (message.runId !== "0") {
      obj.runId = message.runId;
    }
    if (message.nodeId !== "0") {
      obj.nodeId = message.nodeId;
    }
    if (message.nodeConfig) {
      const entries = globalThis.Object.entries(message.nodeConfig) as [string, Scalar][];
      if (entries.length > 0) {
        obj.nodeConfig = {};
        entries.forEach(([k, v]) => {
          obj.nodeConfig[k] = Scalar.toJSON(v);
        });
      }
    }
    if (message.state !== undefined) {
      obj.state = RecordDict.toJSON(message.state);
    }
    if (message.runConfig) {
      const entries = globalThis.Object.entries(message.runConfig) as [string, Scalar][];
      if (entries.length > 0) {
        obj.runConfig = {};
        entries.forEach(([k, v]) => {
          obj.runConfig[k] = Scalar.toJSON(v);
        });
      }
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Context>, I>>(base?: I): Context {
    return Context.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Context>, I>>(object: I): Context {
    const message = createBaseContext();
    message.runId = object.runId ?? "0";
    message.nodeId = object.nodeId ?? "0";
    message.nodeConfig = (globalThis.Object.entries(object.nodeConfig ?? {}) as [string, Scalar][]).reduce(
      (acc: { [key: string]: Scalar }, [key, value]: [string, Scalar]) => {
        if (value !== undefined) {
          acc[key] = Scalar.fromPartial(value);
        }
        return acc;
      },
      {},
    );
    message.state = (object.state !== undefined && object.state !== null)
      ? RecordDict.fromPartial(object.state)
      : undefined;
    message.runConfig = (globalThis.Object.entries(object.runConfig ?? {}) as [string, Scalar][]).reduce(
      (acc: { [key: string]: Scalar }, [key, value]: [string, Scalar]) => {
        if (value !== undefined) {
          acc[key] = Scalar.fromPartial(value);
        }
        return acc;
      },
      {},
    );
    return message;
  },
};

function createBaseContext_NodeConfigEntry(): Context_NodeConfigEntry {
  return { key: "", value: undefined };
}

export const Context_NodeConfigEntry: MessageFns<Context_NodeConfigEntry> = {
  encode(message: Context_NodeConfigEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== undefined) {
      Scalar.encode(message.value, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Context_NodeConfigEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseContext_NodeConfigEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = Scalar.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Context_NodeConfigEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? Scalar.fromJSON(object.value) : undefined,
    };
  },

  toJSON(message: Context_NodeConfigEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== undefined) {
      obj.value = Scalar.toJSON(message.value);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Context_NodeConfigEntry>, I>>(base?: I): Context_NodeConfigEntry {
    return Context_NodeConfigEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Context_NodeConfigEntry>, I>>(object: I): Context_NodeConfigEntry {
    const message = createBaseContext_NodeConfigEntry();
    message.key = object.key ?? "";
    message.value = (object.value !== undefined && object.value !== null)
      ? Scalar.fromPartial(object.value)
      : undefined;
    return message;
  },
};

function createBaseContext_RunConfigEntry(): Context_RunConfigEntry {
  return { key: "", value: undefined };
}

export const Context_RunConfigEntry: MessageFns<Context_RunConfigEntry> = {
  encode(message: Context_RunConfigEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== undefined) {
      Scalar.encode(message.value, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Context_RunConfigEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseContext_RunConfigEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = Scalar.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Context_RunConfigEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? Scalar.fromJSON(object.value) : undefined,
    };
  },

  toJSON(message: Context_RunConfigEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== undefined) {
      obj.value = Scalar.toJSON(message.value);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Context_RunConfigEntry>, I>>(base?: I): Context_RunConfigEntry {
    return Context_RunConfigEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Context_RunConfigEntry>, I>>(object: I): Context_RunConfigEntry {
    const message = createBaseContext_RunConfigEntry();
    message.key = object.key ?? "";
    message.value = (object.value !== undefined && object.value !== null)
      ? Scalar.fromPartial(object.value)
      : undefined;
    return message;
  },
};

function createBaseMetadata(): Metadata {
  return {
    runId: "0",
    messageId: "",
    srcNodeId: "0",
    dstNodeId: "0",
    replyToMessageId: "",
    groupId: "",
    ttl: 0,
    messageType: "",
    createdAt: 0,
  };
}

export const Metadata: MessageFns<Metadata> = {
  encode(message: Metadata, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.runId !== "0") {
      writer.uint32(8).uint64(message.runId);
    }
    if (message.messageId !== "") {
      writer.uint32(18).string(message.messageId);
    }
    if (message.srcNodeId !== "0") {
      writer.uint32(24).uint64(message.srcNodeId);
    }
    if (message.dstNodeId !== "0") {
      writer.uint32(32).uint64(message.dstNodeId);
    }
    if (message.replyToMessageId !== "") {
      writer.uint32(42).string(message.replyToMessageId);
    }
    if (message.groupId !== "") {
      writer.uint32(50).string(message.groupId);
    }
    if (message.ttl !== 0) {
      writer.uint32(57).double(message.ttl);
    }
    if (message.messageType !== "") {
      writer.uint32(66).string(message.messageType);
    }
    if (message.createdAt !== 0) {
      writer.uint32(73).double(message.createdAt);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Metadata {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMetadata();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.runId = reader.uint64().toString();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.messageId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.srcNodeId = reader.uint64().toString();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.dstNodeId = reader.uint64().toString();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.replyToMessageId = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.groupId = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 57) {
            break;
          }

          message.ttl = reader.double();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.messageType = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 73) {
            break;
          }

          message.createdAt = reader.double();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Metadata {
    return {
      runId: isSet(object.runId) ? globalThis.String(object.runId) : "0",
      messageId: isSet(object.messageId) ? globalThis.String(object.messageId) : "",
      srcNodeId: isSet(object.srcNodeId) ? globalThis.String(object.srcNodeId) : "0",
      dstNodeId: isSet(object.dstNodeId) ? globalThis.String(object.dstNodeId) : "0",
      replyToMessageId: isSet(object.replyToMessageId) ? globalThis.String(object.replyToMessageId) : "",
      groupId: isSet(object.groupId) ? globalThis.String(object.groupId) : "",
      ttl: isSet(object.ttl) ? globalThis.Number(object.ttl) : 0,
      messageType: isSet(object.messageType) ? globalThis.String(object.messageType) : "",
      createdAt: isSet(object.createdAt) ? globalThis.Number(object.createdAt) : 0,
    };
  },

  toJSON(message: Metadata): unknown {
    const obj: any = {};
    if (message.runId !== "0") {
      obj.runId = message.runId;
    }
    if (message.messageId !== "") {
      obj.messageId = message.messageId;
    }
    if (message.srcNodeId !== "0") {
      obj.srcNodeId = message.srcNodeId;
    }
    if (message.dstNodeId !== "0") {
      obj.dstNodeId = message.dstNodeId;
    }
    if (message.replyToMessageId !== "") {
      obj.replyToMessageId = message.replyToMessageId;
    }
    if (message.groupId !== "") {
      obj.groupId = message.groupId;
    }
    if (message.ttl !== 0) {
      obj.ttl = message.ttl;
    }
    if (message.messageType !== "") {
      obj.messageType = message.messageType;
    }
    if (message.createdAt !== 0) {
      obj.createdAt = message.createdAt;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Metadata>, I>>(base?: I): Metadata {
    return Metadata.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Metadata>, I>>(object: I): Metadata {
    const message = createBaseMetadata();
    message.runId = object.runId ?? "0";
    message.messageId = object.messageId ?? "";
    message.srcNodeId = object.srcNodeId ?? "0";
    message.dstNodeId = object.dstNodeId ?? "0";
    message.replyToMessageId = object.replyToMessageId ?? "";
    message.groupId = object.groupId ?? "";
    message.ttl = object.ttl ?? 0;
    message.messageType = object.messageType ?? "";
    message.createdAt = object.createdAt ?? 0;
    return message;
  },
};

function createBaseObjectIDs(): ObjectIDs {
  return { objectIds: [] };
}

export const ObjectIDs: MessageFns<ObjectIDs> = {
  encode(message: ObjectIDs, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.objectIds) {
      writer.uint32(10).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ObjectIDs {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseObjectIDs();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.objectIds.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ObjectIDs {
    return {
      objectIds: globalThis.Array.isArray(object?.objectIds)
        ? object.objectIds.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: ObjectIDs): unknown {
    const obj: any = {};
    if (message.objectIds?.length) {
      obj.objectIds = message.objectIds;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ObjectIDs>, I>>(base?: I): ObjectIDs {
    return ObjectIDs.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ObjectIDs>, I>>(object: I): ObjectIDs {
    const message = createBaseObjectIDs();
    message.objectIds = object.objectIds?.map((e) => e) || [];
    return message;
  },
};

function createBaseObjectTree(): ObjectTree {
  return { objectId: "", children: [] };
}

export const ObjectTree: MessageFns<ObjectTree> = {
  encode(message: ObjectTree, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.objectId !== "") {
      writer.uint32(10).string(message.objectId);
    }
    for (const v of message.children) {
      ObjectTree.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ObjectTree {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseObjectTree();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.objectId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.children.push(ObjectTree.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ObjectTree {
    return {
      objectId: isSet(object.objectId) ? globalThis.String(object.objectId) : "",
      children: globalThis.Array.isArray(object?.children)
        ? object.children.map((e: any) => ObjectTree.fromJSON(e))
        : [],
    };
  },

  toJSON(message: ObjectTree): unknown {
    const obj: any = {};
    if (message.objectId !== "") {
      obj.objectId = message.objectId;
    }
    if (message.children?.length) {
      obj.children = message.children.map((e) => ObjectTree.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ObjectTree>, I>>(base?: I): ObjectTree {
    return ObjectTree.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ObjectTree>, I>>(object: I): ObjectTree {
    const message = createBaseObjectTree();
    message.objectId = object.objectId ?? "";
    message.children = object.children?.map((e) => ObjectTree.fromPartial(e)) || [];
    return message;
  },
};

function createBasePushObjectRequest(): PushObjectRequest {
  return { node: undefined, runId: "0", objectId: "", objectContent: new Uint8Array(0) };
}

export const PushObjectRequest: MessageFns<PushObjectRequest> = {
  encode(message: PushObjectRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.node !== undefined) {
      Node.encode(message.node, writer.uint32(10).fork()).join();
    }
    if (message.runId !== "0") {
      writer.uint32(16).uint64(message.runId);
    }
    if (message.objectId !== "") {
      writer.uint32(26).string(message.objectId);
    }
    if (message.objectContent.length !== 0) {
      writer.uint32(34).bytes(message.objectContent);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PushObjectRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePushObjectRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.node = Node.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.runId = reader.uint64().toString();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.objectId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.objectContent = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PushObjectRequest {
    return {
      node: isSet(object.node) ? Node.fromJSON(object.node) : undefined,
      runId: isSet(object.runId) ? globalThis.String(object.runId) : "0",
      objectId: isSet(object.objectId) ? globalThis.String(object.objectId) : "",
      objectContent: isSet(object.objectContent) ? bytesFromBase64(object.objectContent) : new Uint8Array(0),
    };
  },

  toJSON(message: PushObjectRequest): unknown {
    const obj: any = {};
    if (message.node !== undefined) {
      obj.node = Node.toJSON(message.node);
    }
    if (message.runId !== "0") {
      obj.runId = message.runId;
    }
    if (message.objectId !== "") {
      obj.objectId = message.objectId;
    }
    if (message.objectContent.length !== 0) {
      obj.objectContent = base64FromBytes(message.objectContent);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PushObjectRequest>, I>>(base?: I): PushObjectRequest {
    return PushObjectRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PushObjectRequest>, I>>(object: I): PushObjectRequest {
    const message = createBasePushObjectRequest();
    message.node = (object.node !== undefined && object.node !== null) ? Node.fromPartial(object.node) : undefined;
    message.runId = object.runId ?? "0";
    message.objectId = object.objectId ?? "";
    message.objectContent = object.objectContent ?? new Uint8Array(0);
    return message;
  },
};

function createBasePushObjectResponse(): PushObjectResponse {
  return { stored: false };
}

export const PushObjectResponse: MessageFns<PushObjectResponse> = {
  encode(message: PushObjectResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.stored !== false) {
      writer.uint32(8).bool(message.stored);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PushObjectResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePushObjectResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.stored = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PushObjectResponse {
    return { stored: isSet(object.stored) ? globalThis.Boolean(object.stored) : false };
  },

  toJSON(message: PushObjectResponse): unknown {
    const obj: any = {};
    if (message.stored !== false) {
      obj.stored = message.stored;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PushObjectResponse>, I>>(base?: I): PushObjectResponse {
    return PushObjectResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PushObjectResponse>, I>>(object: I): PushObjectResponse {
    const message = createBasePushObjectResponse();
    message.stored = object.stored ?? false;
    return message;
  },
};

function createBasePullObjectRequest(): PullObjectRequest {
  return { node: undefined, runId: "0", objectId: "" };
}

export const PullObjectRequest: MessageFns<PullObjectRequest> = {
  encode(message: PullObjectRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.node !== undefined) {
      Node.encode(message.node, writer.uint32(10).fork()).join();
    }
    if (message.runId !== "0") {
      writer.uint32(16).uint64(message.runId);
    }
    if (message.objectId !== "") {
      writer.uint32(26).string(message.objectId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PullObjectRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePullObjectRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.node = Node.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.runId = reader.uint64().toString();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.objectId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PullObjectRequest {
    return {
      node: isSet(object.node) ? Node.fromJSON(object.node) : undefined,
      runId: isSet(object.runId) ? globalThis.String(object.runId) : "0",
      objectId: isSet(object.objectId) ? globalThis.String(object.objectId) : "",
    };
  },

  toJSON(message: PullObjectRequest): unknown {
    const obj: any = {};
    if (message.node !== undefined) {
      obj.node = Node.toJSON(message.node);
    }
    if (message.runId !== "0") {
      obj.runId = message.runId;
    }
    if (message.objectId !== "") {
      obj.objectId = message.objectId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PullObjectRequest>, I>>(base?: I): PullObjectRequest {
    return PullObjectRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PullObjectRequest>, I>>(object: I): PullObjectRequest {
    const message = createBasePullObjectRequest();
    message.node = (object.node !== undefined && object.node !== null) ? Node.fromPartial(object.node) : undefined;
    message.runId = object.runId ?? "0";
    message.objectId = object.objectId ?? "";
    return message;
  },
};

function createBasePullObjectResponse(): PullObjectResponse {
  return { objectFound: false, objectAvailable: false, objectContent: new Uint8Array(0) };
}

export const PullObjectResponse: MessageFns<PullObjectResponse> = {
  encode(message: PullObjectResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.objectFound !== false) {
      writer.uint32(8).bool(message.objectFound);
    }
    if (message.objectAvailable !== false) {
      writer.uint32(16).bool(message.objectAvailable);
    }
    if (message.objectContent.length !== 0) {
      writer.uint32(26).bytes(message.objectContent);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PullObjectResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePullObjectResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.objectFound = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.objectAvailable = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.objectContent = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PullObjectResponse {
    return {
      objectFound: isSet(object.objectFound) ? globalThis.Boolean(object.objectFound) : false,
      objectAvailable: isSet(object.objectAvailable) ? globalThis.Boolean(object.objectAvailable) : false,
      objectContent: isSet(object.objectContent) ? bytesFromBase64(object.objectContent) : new Uint8Array(0),
    };
  },

  toJSON(message: PullObjectResponse): unknown {
    const obj: any = {};
    if (message.objectFound !== false) {
      obj.objectFound = message.objectFound;
    }
    if (message.objectAvailable !== false) {
      obj.objectAvailable = message.objectAvailable;
    }
    if (message.objectContent.length !== 0) {
      obj.objectContent = base64FromBytes(message.objectContent);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PullObjectResponse>, I>>(base?: I): PullObjectResponse {
    return PullObjectResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PullObjectResponse>, I>>(object: I): PullObjectResponse {
    const message = createBasePullObjectResponse();
    message.objectFound = object.objectFound ?? false;
    message.objectAvailable = object.objectAvailable ?? false;
    message.objectContent = object.objectContent ?? new Uint8Array(0);
    return message;
  },
};

function createBaseConfirmMessageReceivedRequest(): ConfirmMessageReceivedRequest {
  return { node: undefined, runId: "0", messageObjectId: "" };
}

export const ConfirmMessageReceivedRequest: MessageFns<ConfirmMessageReceivedRequest> = {
  encode(message: ConfirmMessageReceivedRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.node !== undefined) {
      Node.encode(message.node, writer.uint32(10).fork()).join();
    }
    if (message.runId !== "0") {
      writer.uint32(16).uint64(message.runId);
    }
    if (message.messageObjectId !== "") {
      writer.uint32(26).string(message.messageObjectId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ConfirmMessageReceivedRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseConfirmMessageReceivedRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.node = Node.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.runId = reader.uint64().toString();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.messageObjectId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ConfirmMessageReceivedRequest {
    return {
      node: isSet(object.node) ? Node.fromJSON(object.node) : undefined,
      runId: isSet(object.runId) ? globalThis.String(object.runId) : "0",
      messageObjectId: isSet(object.messageObjectId) ? globalThis.String(object.messageObjectId) : "",
    };
  },

  toJSON(message: ConfirmMessageReceivedRequest): unknown {
    const obj: any = {};
    if (message.node !== undefined) {
      obj.node = Node.toJSON(message.node);
    }
    if (message.runId !== "0") {
      obj.runId = message.runId;
    }
    if (message.messageObjectId !== "") {
      obj.messageObjectId = message.messageObjectId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ConfirmMessageReceivedRequest>, I>>(base?: I): ConfirmMessageReceivedRequest {
    return ConfirmMessageReceivedRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ConfirmMessageReceivedRequest>, I>>(
    object: I,
  ): ConfirmMessageReceivedRequest {
    const message = createBaseConfirmMessageReceivedRequest();
    message.node = (object.node !== undefined && object.node !== null) ? Node.fromPartial(object.node) : undefined;
    message.runId = object.runId ?? "0";
    message.messageObjectId = object.messageObjectId ?? "";
    return message;
  },
};

function createBaseConfirmMessageReceivedResponse(): ConfirmMessageReceivedResponse {
  return {};
}

export const ConfirmMessageReceivedResponse: MessageFns<ConfirmMessageReceivedResponse> = {
  encode(_: ConfirmMessageReceivedResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ConfirmMessageReceivedResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseConfirmMessageReceivedResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): ConfirmMessageReceivedResponse {
    return {};
  },

  toJSON(_: ConfirmMessageReceivedResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<ConfirmMessageReceivedResponse>, I>>(base?: I): ConfirmMessageReceivedResponse {
    return ConfirmMessageReceivedResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ConfirmMessageReceivedResponse>, I>>(_: I): ConfirmMessageReceivedResponse {
    const message = createBaseConfirmMessageReceivedResponse();
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return globalThis.Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
