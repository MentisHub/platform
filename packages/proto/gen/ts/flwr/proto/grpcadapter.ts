// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.10.1
//   protoc               v3.21.12
// source: flwr/proto/grpcadapter.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import {
  type CallOptions,
  type ChannelCredentials,
  Client,
  type ClientOptions,
  type ClientUnaryCall,
  type handleUnaryCall,
  makeGenericClientConstructor,
  type Metadata,
  type ServiceError,
  type UntypedServiceImplementation,
} from "@grpc/grpc-js";

export const protobufPackage = "flwr.proto";

export interface MessageContainer {
  metadata: { [key: string]: string };
  grpcMessageName: string;
  grpcMessageContent: Uint8Array;
}

export interface MessageContainer_MetadataEntry {
  key: string;
  value: string;
}

function createBaseMessageContainer(): MessageContainer {
  return { metadata: {}, grpcMessageName: "", grpcMessageContent: new Uint8Array(0) };
}

export const MessageContainer: MessageFns<MessageContainer> = {
  encode(message: MessageContainer, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    globalThis.Object.entries(message.metadata).forEach(([key, value]: [string, string]) => {
      MessageContainer_MetadataEntry.encode({ key: key as any, value }, writer.uint32(10).fork()).join();
    });
    if (message.grpcMessageName !== "") {
      writer.uint32(18).string(message.grpcMessageName);
    }
    if (message.grpcMessageContent.length !== 0) {
      writer.uint32(26).bytes(message.grpcMessageContent);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MessageContainer {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMessageContainer();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          const entry1 = MessageContainer_MetadataEntry.decode(reader, reader.uint32());
          if (entry1.value !== undefined) {
            message.metadata[entry1.key] = entry1.value;
          }
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.grpcMessageName = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.grpcMessageContent = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MessageContainer {
    return {
      metadata: isObject(object.metadata)
        ? (globalThis.Object.entries(object.metadata) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : {},
      grpcMessageName: isSet(object.grpcMessageName) ? globalThis.String(object.grpcMessageName) : "",
      grpcMessageContent: isSet(object.grpcMessageContent)
        ? bytesFromBase64(object.grpcMessageContent)
        : new Uint8Array(0),
    };
  },

  toJSON(message: MessageContainer): unknown {
    const obj: any = {};
    if (message.metadata) {
      const entries = globalThis.Object.entries(message.metadata) as [string, string][];
      if (entries.length > 0) {
        obj.metadata = {};
        entries.forEach(([k, v]) => {
          obj.metadata[k] = v;
        });
      }
    }
    if (message.grpcMessageName !== "") {
      obj.grpcMessageName = message.grpcMessageName;
    }
    if (message.grpcMessageContent.length !== 0) {
      obj.grpcMessageContent = base64FromBytes(message.grpcMessageContent);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MessageContainer>, I>>(base?: I): MessageContainer {
    return MessageContainer.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MessageContainer>, I>>(object: I): MessageContainer {
    const message = createBaseMessageContainer();
    message.metadata = (globalThis.Object.entries(object.metadata ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    message.grpcMessageName = object.grpcMessageName ?? "";
    message.grpcMessageContent = object.grpcMessageContent ?? new Uint8Array(0);
    return message;
  },
};

function createBaseMessageContainer_MetadataEntry(): MessageContainer_MetadataEntry {
  return { key: "", value: "" };
}

export const MessageContainer_MetadataEntry: MessageFns<MessageContainer_MetadataEntry> = {
  encode(message: MessageContainer_MetadataEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MessageContainer_MetadataEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMessageContainer_MetadataEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MessageContainer_MetadataEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: MessageContainer_MetadataEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MessageContainer_MetadataEntry>, I>>(base?: I): MessageContainer_MetadataEntry {
    return MessageContainer_MetadataEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MessageContainer_MetadataEntry>, I>>(
    object: I,
  ): MessageContainer_MetadataEntry {
    const message = createBaseMessageContainer_MetadataEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

export type GrpcAdapterService = typeof GrpcAdapterService;
export const GrpcAdapterService = {
  sendReceive: {
    path: "/flwr.proto.GrpcAdapter/SendReceive",
    requestStream: false,
    responseStream: false,
    requestSerialize: (value: MessageContainer): Buffer => Buffer.from(MessageContainer.encode(value).finish()),
    requestDeserialize: (value: Buffer): MessageContainer => MessageContainer.decode(value),
    responseSerialize: (value: MessageContainer): Buffer => Buffer.from(MessageContainer.encode(value).finish()),
    responseDeserialize: (value: Buffer): MessageContainer => MessageContainer.decode(value),
  },
} as const;

export interface GrpcAdapterServer extends UntypedServiceImplementation {
  sendReceive: handleUnaryCall<MessageContainer, MessageContainer>;
}

export interface GrpcAdapterClient extends Client {
  sendReceive(
    request: MessageContainer,
    callback: (error: ServiceError | null, response: MessageContainer) => void,
  ): ClientUnaryCall;
  sendReceive(
    request: MessageContainer,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: MessageContainer) => void,
  ): ClientUnaryCall;
  sendReceive(
    request: MessageContainer,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: MessageContainer) => void,
  ): ClientUnaryCall;
}

export const GrpcAdapterClient = makeGenericClientConstructor(
  GrpcAdapterService,
  "flwr.proto.GrpcAdapter",
) as unknown as {
  new (address: string, credentials: ChannelCredentials, options?: Partial<ClientOptions>): GrpcAdapterClient;
  service: typeof GrpcAdapterService;
  serviceName: string;
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return globalThis.Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
