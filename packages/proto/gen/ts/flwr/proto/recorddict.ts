// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.10.1
//   protoc               v3.21.12
// source: flwr/proto/recorddict.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "flwr.proto";

export interface DoubleList {
  vals: number[];
}

export interface SintList {
  vals: string[];
}

export interface UintList {
  vals: string[];
}

export interface BoolList {
  vals: boolean[];
}

export interface StringList {
  vals: string[];
}

export interface BytesList {
  vals: Uint8Array[];
}

export interface Array {
  dtype: string;
  shape: number[];
  stype: string;
  data: Uint8Array;
}

export interface MetricRecordValue {
  /** Single element */
  double?: number | undefined;
  sint64?: string | undefined;
  uint64?:
    | string
    | undefined;
  /** List types */
  doubleList?: DoubleList | undefined;
  sintList?: SintList | undefined;
  uintList?: UintList | undefined;
}

export interface ConfigRecordValue {
  /** Single element */
  double?: number | undefined;
  sint64?: string | undefined;
  uint64?: string | undefined;
  bool?: boolean | undefined;
  string?: string | undefined;
  bytes?:
    | Uint8Array
    | undefined;
  /** List types */
  doubleList?: DoubleList | undefined;
  sintList?: SintList | undefined;
  uintList?: UintList | undefined;
  boolList?: BoolList | undefined;
  stringList?: StringList | undefined;
  bytesList?: BytesList | undefined;
}

export interface ArrayRecord {
  items: ArrayRecord_Item[];
}

export interface ArrayRecord_Item {
  key: string;
  value: Array | undefined;
}

export interface MetricRecord {
  items: MetricRecord_Item[];
}

export interface MetricRecord_Item {
  key: string;
  value: MetricRecordValue | undefined;
}

export interface ConfigRecord {
  items: ConfigRecord_Item[];
}

export interface ConfigRecord_Item {
  key: string;
  value: ConfigRecordValue | undefined;
}

export interface RecordDict {
  items: RecordDict_Item[];
}

export interface RecordDict_Item {
  key: string;
  arrayRecord?: ArrayRecord | undefined;
  metricRecord?: MetricRecord | undefined;
  configRecord?: ConfigRecord | undefined;
}

function createBaseDoubleList(): DoubleList {
  return { vals: [] };
}

export const DoubleList: MessageFns<DoubleList> = {
  encode(message: DoubleList, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    writer.uint32(10).fork();
    for (const v of message.vals) {
      writer.double(v);
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DoubleList {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDoubleList();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag === 9) {
            message.vals.push(reader.double());

            continue;
          }

          if (tag === 10) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.vals.push(reader.double());
            }

            continue;
          }

          break;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DoubleList {
    return { vals: globalThis.Array.isArray(object?.vals) ? object.vals.map((e: any) => globalThis.Number(e)) : [] };
  },

  toJSON(message: DoubleList): unknown {
    const obj: any = {};
    if (message.vals?.length) {
      obj.vals = message.vals;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DoubleList>, I>>(base?: I): DoubleList {
    return DoubleList.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DoubleList>, I>>(object: I): DoubleList {
    const message = createBaseDoubleList();
    message.vals = object.vals?.map((e) => e) || [];
    return message;
  },
};

function createBaseSintList(): SintList {
  return { vals: [] };
}

export const SintList: MessageFns<SintList> = {
  encode(message: SintList, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    writer.uint32(10).fork();
    for (const v of message.vals) {
      writer.sint64(v);
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SintList {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSintList();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag === 8) {
            message.vals.push(reader.sint64().toString());

            continue;
          }

          if (tag === 10) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.vals.push(reader.sint64().toString());
            }

            continue;
          }

          break;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SintList {
    return { vals: globalThis.Array.isArray(object?.vals) ? object.vals.map((e: any) => globalThis.String(e)) : [] };
  },

  toJSON(message: SintList): unknown {
    const obj: any = {};
    if (message.vals?.length) {
      obj.vals = message.vals;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SintList>, I>>(base?: I): SintList {
    return SintList.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SintList>, I>>(object: I): SintList {
    const message = createBaseSintList();
    message.vals = object.vals?.map((e) => e) || [];
    return message;
  },
};

function createBaseUintList(): UintList {
  return { vals: [] };
}

export const UintList: MessageFns<UintList> = {
  encode(message: UintList, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    writer.uint32(10).fork();
    for (const v of message.vals) {
      writer.uint64(v);
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UintList {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUintList();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag === 8) {
            message.vals.push(reader.uint64().toString());

            continue;
          }

          if (tag === 10) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.vals.push(reader.uint64().toString());
            }

            continue;
          }

          break;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UintList {
    return { vals: globalThis.Array.isArray(object?.vals) ? object.vals.map((e: any) => globalThis.String(e)) : [] };
  },

  toJSON(message: UintList): unknown {
    const obj: any = {};
    if (message.vals?.length) {
      obj.vals = message.vals;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UintList>, I>>(base?: I): UintList {
    return UintList.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UintList>, I>>(object: I): UintList {
    const message = createBaseUintList();
    message.vals = object.vals?.map((e) => e) || [];
    return message;
  },
};

function createBaseBoolList(): BoolList {
  return { vals: [] };
}

export const BoolList: MessageFns<BoolList> = {
  encode(message: BoolList, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    writer.uint32(10).fork();
    for (const v of message.vals) {
      writer.bool(v);
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BoolList {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBoolList();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag === 8) {
            message.vals.push(reader.bool());

            continue;
          }

          if (tag === 10) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.vals.push(reader.bool());
            }

            continue;
          }

          break;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BoolList {
    return { vals: globalThis.Array.isArray(object?.vals) ? object.vals.map((e: any) => globalThis.Boolean(e)) : [] };
  },

  toJSON(message: BoolList): unknown {
    const obj: any = {};
    if (message.vals?.length) {
      obj.vals = message.vals;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BoolList>, I>>(base?: I): BoolList {
    return BoolList.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BoolList>, I>>(object: I): BoolList {
    const message = createBaseBoolList();
    message.vals = object.vals?.map((e) => e) || [];
    return message;
  },
};

function createBaseStringList(): StringList {
  return { vals: [] };
}

export const StringList: MessageFns<StringList> = {
  encode(message: StringList, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.vals) {
      writer.uint32(10).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StringList {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStringList();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.vals.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StringList {
    return { vals: globalThis.Array.isArray(object?.vals) ? object.vals.map((e: any) => globalThis.String(e)) : [] };
  },

  toJSON(message: StringList): unknown {
    const obj: any = {};
    if (message.vals?.length) {
      obj.vals = message.vals;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StringList>, I>>(base?: I): StringList {
    return StringList.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StringList>, I>>(object: I): StringList {
    const message = createBaseStringList();
    message.vals = object.vals?.map((e) => e) || [];
    return message;
  },
};

function createBaseBytesList(): BytesList {
  return { vals: [] };
}

export const BytesList: MessageFns<BytesList> = {
  encode(message: BytesList, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.vals) {
      writer.uint32(10).bytes(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BytesList {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBytesList();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.vals.push(reader.bytes());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BytesList {
    return { vals: globalThis.Array.isArray(object?.vals) ? object.vals.map((e: any) => bytesFromBase64(e)) : [] };
  },

  toJSON(message: BytesList): unknown {
    const obj: any = {};
    if (message.vals?.length) {
      obj.vals = message.vals.map((e) => base64FromBytes(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BytesList>, I>>(base?: I): BytesList {
    return BytesList.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BytesList>, I>>(object: I): BytesList {
    const message = createBaseBytesList();
    message.vals = object.vals?.map((e) => e) || [];
    return message;
  },
};

function createBaseArray(): Array {
  return { dtype: "", shape: [], stype: "", data: new Uint8Array(0) };
}

export const Array: MessageFns<Array> = {
  encode(message: Array, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.dtype !== "") {
      writer.uint32(10).string(message.dtype);
    }
    writer.uint32(18).fork();
    for (const v of message.shape) {
      writer.int32(v);
    }
    writer.join();
    if (message.stype !== "") {
      writer.uint32(26).string(message.stype);
    }
    if (message.data.length !== 0) {
      writer.uint32(34).bytes(message.data);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Array {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseArray();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.dtype = reader.string();
          continue;
        }
        case 2: {
          if (tag === 16) {
            message.shape.push(reader.int32());

            continue;
          }

          if (tag === 18) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.shape.push(reader.int32());
            }

            continue;
          }

          break;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.stype = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.data = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Array {
    return {
      dtype: isSet(object.dtype) ? globalThis.String(object.dtype) : "",
      shape: globalThis.Array.isArray(object?.shape) ? object.shape.map((e: any) => globalThis.Number(e)) : [],
      stype: isSet(object.stype) ? globalThis.String(object.stype) : "",
      data: isSet(object.data) ? bytesFromBase64(object.data) : new Uint8Array(0),
    };
  },

  toJSON(message: Array): unknown {
    const obj: any = {};
    if (message.dtype !== "") {
      obj.dtype = message.dtype;
    }
    if (message.shape?.length) {
      obj.shape = message.shape.map((e) => Math.round(e));
    }
    if (message.stype !== "") {
      obj.stype = message.stype;
    }
    if (message.data.length !== 0) {
      obj.data = base64FromBytes(message.data);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Array>, I>>(base?: I): Array {
    return Array.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Array>, I>>(object: I): Array {
    const message = createBaseArray();
    message.dtype = object.dtype ?? "";
    message.shape = object.shape?.map((e) => e) || [];
    message.stype = object.stype ?? "";
    message.data = object.data ?? new Uint8Array(0);
    return message;
  },
};

function createBaseMetricRecordValue(): MetricRecordValue {
  return {
    double: undefined,
    sint64: undefined,
    uint64: undefined,
    doubleList: undefined,
    sintList: undefined,
    uintList: undefined,
  };
}

export const MetricRecordValue: MessageFns<MetricRecordValue> = {
  encode(message: MetricRecordValue, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.double !== undefined) {
      writer.uint32(9).double(message.double);
    }
    if (message.sint64 !== undefined) {
      writer.uint32(16).sint64(message.sint64);
    }
    if (message.uint64 !== undefined) {
      writer.uint32(24).uint64(message.uint64);
    }
    if (message.doubleList !== undefined) {
      DoubleList.encode(message.doubleList, writer.uint32(170).fork()).join();
    }
    if (message.sintList !== undefined) {
      SintList.encode(message.sintList, writer.uint32(178).fork()).join();
    }
    if (message.uintList !== undefined) {
      UintList.encode(message.uintList, writer.uint32(186).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MetricRecordValue {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMetricRecordValue();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 9) {
            break;
          }

          message.double = reader.double();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.sint64 = reader.sint64().toString();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.uint64 = reader.uint64().toString();
          continue;
        }
        case 21: {
          if (tag !== 170) {
            break;
          }

          message.doubleList = DoubleList.decode(reader, reader.uint32());
          continue;
        }
        case 22: {
          if (tag !== 178) {
            break;
          }

          message.sintList = SintList.decode(reader, reader.uint32());
          continue;
        }
        case 23: {
          if (tag !== 186) {
            break;
          }

          message.uintList = UintList.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MetricRecordValue {
    return {
      double: isSet(object.double) ? globalThis.Number(object.double) : undefined,
      sint64: isSet(object.sint64) ? globalThis.String(object.sint64) : undefined,
      uint64: isSet(object.uint64) ? globalThis.String(object.uint64) : undefined,
      doubleList: isSet(object.doubleList) ? DoubleList.fromJSON(object.doubleList) : undefined,
      sintList: isSet(object.sintList) ? SintList.fromJSON(object.sintList) : undefined,
      uintList: isSet(object.uintList) ? UintList.fromJSON(object.uintList) : undefined,
    };
  },

  toJSON(message: MetricRecordValue): unknown {
    const obj: any = {};
    if (message.double !== undefined) {
      obj.double = message.double;
    }
    if (message.sint64 !== undefined) {
      obj.sint64 = message.sint64;
    }
    if (message.uint64 !== undefined) {
      obj.uint64 = message.uint64;
    }
    if (message.doubleList !== undefined) {
      obj.doubleList = DoubleList.toJSON(message.doubleList);
    }
    if (message.sintList !== undefined) {
      obj.sintList = SintList.toJSON(message.sintList);
    }
    if (message.uintList !== undefined) {
      obj.uintList = UintList.toJSON(message.uintList);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MetricRecordValue>, I>>(base?: I): MetricRecordValue {
    return MetricRecordValue.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MetricRecordValue>, I>>(object: I): MetricRecordValue {
    const message = createBaseMetricRecordValue();
    message.double = object.double ?? undefined;
    message.sint64 = object.sint64 ?? undefined;
    message.uint64 = object.uint64 ?? undefined;
    message.doubleList = (object.doubleList !== undefined && object.doubleList !== null)
      ? DoubleList.fromPartial(object.doubleList)
      : undefined;
    message.sintList = (object.sintList !== undefined && object.sintList !== null)
      ? SintList.fromPartial(object.sintList)
      : undefined;
    message.uintList = (object.uintList !== undefined && object.uintList !== null)
      ? UintList.fromPartial(object.uintList)
      : undefined;
    return message;
  },
};

function createBaseConfigRecordValue(): ConfigRecordValue {
  return {
    double: undefined,
    sint64: undefined,
    uint64: undefined,
    bool: undefined,
    string: undefined,
    bytes: undefined,
    doubleList: undefined,
    sintList: undefined,
    uintList: undefined,
    boolList: undefined,
    stringList: undefined,
    bytesList: undefined,
  };
}

export const ConfigRecordValue: MessageFns<ConfigRecordValue> = {
  encode(message: ConfigRecordValue, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.double !== undefined) {
      writer.uint32(9).double(message.double);
    }
    if (message.sint64 !== undefined) {
      writer.uint32(16).sint64(message.sint64);
    }
    if (message.uint64 !== undefined) {
      writer.uint32(24).uint64(message.uint64);
    }
    if (message.bool !== undefined) {
      writer.uint32(32).bool(message.bool);
    }
    if (message.string !== undefined) {
      writer.uint32(42).string(message.string);
    }
    if (message.bytes !== undefined) {
      writer.uint32(50).bytes(message.bytes);
    }
    if (message.doubleList !== undefined) {
      DoubleList.encode(message.doubleList, writer.uint32(170).fork()).join();
    }
    if (message.sintList !== undefined) {
      SintList.encode(message.sintList, writer.uint32(178).fork()).join();
    }
    if (message.uintList !== undefined) {
      UintList.encode(message.uintList, writer.uint32(186).fork()).join();
    }
    if (message.boolList !== undefined) {
      BoolList.encode(message.boolList, writer.uint32(194).fork()).join();
    }
    if (message.stringList !== undefined) {
      StringList.encode(message.stringList, writer.uint32(202).fork()).join();
    }
    if (message.bytesList !== undefined) {
      BytesList.encode(message.bytesList, writer.uint32(210).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ConfigRecordValue {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseConfigRecordValue();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 9) {
            break;
          }

          message.double = reader.double();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.sint64 = reader.sint64().toString();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.uint64 = reader.uint64().toString();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.bool = reader.bool();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.string = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.bytes = reader.bytes();
          continue;
        }
        case 21: {
          if (tag !== 170) {
            break;
          }

          message.doubleList = DoubleList.decode(reader, reader.uint32());
          continue;
        }
        case 22: {
          if (tag !== 178) {
            break;
          }

          message.sintList = SintList.decode(reader, reader.uint32());
          continue;
        }
        case 23: {
          if (tag !== 186) {
            break;
          }

          message.uintList = UintList.decode(reader, reader.uint32());
          continue;
        }
        case 24: {
          if (tag !== 194) {
            break;
          }

          message.boolList = BoolList.decode(reader, reader.uint32());
          continue;
        }
        case 25: {
          if (tag !== 202) {
            break;
          }

          message.stringList = StringList.decode(reader, reader.uint32());
          continue;
        }
        case 26: {
          if (tag !== 210) {
            break;
          }

          message.bytesList = BytesList.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ConfigRecordValue {
    return {
      double: isSet(object.double) ? globalThis.Number(object.double) : undefined,
      sint64: isSet(object.sint64) ? globalThis.String(object.sint64) : undefined,
      uint64: isSet(object.uint64) ? globalThis.String(object.uint64) : undefined,
      bool: isSet(object.bool) ? globalThis.Boolean(object.bool) : undefined,
      string: isSet(object.string) ? globalThis.String(object.string) : undefined,
      bytes: isSet(object.bytes) ? bytesFromBase64(object.bytes) : undefined,
      doubleList: isSet(object.doubleList) ? DoubleList.fromJSON(object.doubleList) : undefined,
      sintList: isSet(object.sintList) ? SintList.fromJSON(object.sintList) : undefined,
      uintList: isSet(object.uintList) ? UintList.fromJSON(object.uintList) : undefined,
      boolList: isSet(object.boolList) ? BoolList.fromJSON(object.boolList) : undefined,
      stringList: isSet(object.stringList) ? StringList.fromJSON(object.stringList) : undefined,
      bytesList: isSet(object.bytesList) ? BytesList.fromJSON(object.bytesList) : undefined,
    };
  },

  toJSON(message: ConfigRecordValue): unknown {
    const obj: any = {};
    if (message.double !== undefined) {
      obj.double = message.double;
    }
    if (message.sint64 !== undefined) {
      obj.sint64 = message.sint64;
    }
    if (message.uint64 !== undefined) {
      obj.uint64 = message.uint64;
    }
    if (message.bool !== undefined) {
      obj.bool = message.bool;
    }
    if (message.string !== undefined) {
      obj.string = message.string;
    }
    if (message.bytes !== undefined) {
      obj.bytes = base64FromBytes(message.bytes);
    }
    if (message.doubleList !== undefined) {
      obj.doubleList = DoubleList.toJSON(message.doubleList);
    }
    if (message.sintList !== undefined) {
      obj.sintList = SintList.toJSON(message.sintList);
    }
    if (message.uintList !== undefined) {
      obj.uintList = UintList.toJSON(message.uintList);
    }
    if (message.boolList !== undefined) {
      obj.boolList = BoolList.toJSON(message.boolList);
    }
    if (message.stringList !== undefined) {
      obj.stringList = StringList.toJSON(message.stringList);
    }
    if (message.bytesList !== undefined) {
      obj.bytesList = BytesList.toJSON(message.bytesList);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ConfigRecordValue>, I>>(base?: I): ConfigRecordValue {
    return ConfigRecordValue.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ConfigRecordValue>, I>>(object: I): ConfigRecordValue {
    const message = createBaseConfigRecordValue();
    message.double = object.double ?? undefined;
    message.sint64 = object.sint64 ?? undefined;
    message.uint64 = object.uint64 ?? undefined;
    message.bool = object.bool ?? undefined;
    message.string = object.string ?? undefined;
    message.bytes = object.bytes ?? undefined;
    message.doubleList = (object.doubleList !== undefined && object.doubleList !== null)
      ? DoubleList.fromPartial(object.doubleList)
      : undefined;
    message.sintList = (object.sintList !== undefined && object.sintList !== null)
      ? SintList.fromPartial(object.sintList)
      : undefined;
    message.uintList = (object.uintList !== undefined && object.uintList !== null)
      ? UintList.fromPartial(object.uintList)
      : undefined;
    message.boolList = (object.boolList !== undefined && object.boolList !== null)
      ? BoolList.fromPartial(object.boolList)
      : undefined;
    message.stringList = (object.stringList !== undefined && object.stringList !== null)
      ? StringList.fromPartial(object.stringList)
      : undefined;
    message.bytesList = (object.bytesList !== undefined && object.bytesList !== null)
      ? BytesList.fromPartial(object.bytesList)
      : undefined;
    return message;
  },
};

function createBaseArrayRecord(): ArrayRecord {
  return { items: [] };
}

export const ArrayRecord: MessageFns<ArrayRecord> = {
  encode(message: ArrayRecord, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.items) {
      ArrayRecord_Item.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ArrayRecord {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseArrayRecord();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.items.push(ArrayRecord_Item.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ArrayRecord {
    return {
      items: globalThis.Array.isArray(object?.items) ? object.items.map((e: any) => ArrayRecord_Item.fromJSON(e)) : [],
    };
  },

  toJSON(message: ArrayRecord): unknown {
    const obj: any = {};
    if (message.items?.length) {
      obj.items = message.items.map((e) => ArrayRecord_Item.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ArrayRecord>, I>>(base?: I): ArrayRecord {
    return ArrayRecord.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ArrayRecord>, I>>(object: I): ArrayRecord {
    const message = createBaseArrayRecord();
    message.items = object.items?.map((e) => ArrayRecord_Item.fromPartial(e)) || [];
    return message;
  },
};

function createBaseArrayRecord_Item(): ArrayRecord_Item {
  return { key: "", value: undefined };
}

export const ArrayRecord_Item: MessageFns<ArrayRecord_Item> = {
  encode(message: ArrayRecord_Item, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== undefined) {
      Array.encode(message.value, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ArrayRecord_Item {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseArrayRecord_Item();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = Array.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ArrayRecord_Item {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? Array.fromJSON(object.value) : undefined,
    };
  },

  toJSON(message: ArrayRecord_Item): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== undefined) {
      obj.value = Array.toJSON(message.value);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ArrayRecord_Item>, I>>(base?: I): ArrayRecord_Item {
    return ArrayRecord_Item.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ArrayRecord_Item>, I>>(object: I): ArrayRecord_Item {
    const message = createBaseArrayRecord_Item();
    message.key = object.key ?? "";
    message.value = (object.value !== undefined && object.value !== null) ? Array.fromPartial(object.value) : undefined;
    return message;
  },
};

function createBaseMetricRecord(): MetricRecord {
  return { items: [] };
}

export const MetricRecord: MessageFns<MetricRecord> = {
  encode(message: MetricRecord, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.items) {
      MetricRecord_Item.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MetricRecord {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMetricRecord();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.items.push(MetricRecord_Item.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MetricRecord {
    return {
      items: globalThis.Array.isArray(object?.items) ? object.items.map((e: any) => MetricRecord_Item.fromJSON(e)) : [],
    };
  },

  toJSON(message: MetricRecord): unknown {
    const obj: any = {};
    if (message.items?.length) {
      obj.items = message.items.map((e) => MetricRecord_Item.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MetricRecord>, I>>(base?: I): MetricRecord {
    return MetricRecord.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MetricRecord>, I>>(object: I): MetricRecord {
    const message = createBaseMetricRecord();
    message.items = object.items?.map((e) => MetricRecord_Item.fromPartial(e)) || [];
    return message;
  },
};

function createBaseMetricRecord_Item(): MetricRecord_Item {
  return { key: "", value: undefined };
}

export const MetricRecord_Item: MessageFns<MetricRecord_Item> = {
  encode(message: MetricRecord_Item, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== undefined) {
      MetricRecordValue.encode(message.value, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MetricRecord_Item {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMetricRecord_Item();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = MetricRecordValue.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MetricRecord_Item {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? MetricRecordValue.fromJSON(object.value) : undefined,
    };
  },

  toJSON(message: MetricRecord_Item): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== undefined) {
      obj.value = MetricRecordValue.toJSON(message.value);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MetricRecord_Item>, I>>(base?: I): MetricRecord_Item {
    return MetricRecord_Item.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MetricRecord_Item>, I>>(object: I): MetricRecord_Item {
    const message = createBaseMetricRecord_Item();
    message.key = object.key ?? "";
    message.value = (object.value !== undefined && object.value !== null)
      ? MetricRecordValue.fromPartial(object.value)
      : undefined;
    return message;
  },
};

function createBaseConfigRecord(): ConfigRecord {
  return { items: [] };
}

export const ConfigRecord: MessageFns<ConfigRecord> = {
  encode(message: ConfigRecord, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.items) {
      ConfigRecord_Item.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ConfigRecord {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseConfigRecord();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.items.push(ConfigRecord_Item.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ConfigRecord {
    return {
      items: globalThis.Array.isArray(object?.items) ? object.items.map((e: any) => ConfigRecord_Item.fromJSON(e)) : [],
    };
  },

  toJSON(message: ConfigRecord): unknown {
    const obj: any = {};
    if (message.items?.length) {
      obj.items = message.items.map((e) => ConfigRecord_Item.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ConfigRecord>, I>>(base?: I): ConfigRecord {
    return ConfigRecord.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ConfigRecord>, I>>(object: I): ConfigRecord {
    const message = createBaseConfigRecord();
    message.items = object.items?.map((e) => ConfigRecord_Item.fromPartial(e)) || [];
    return message;
  },
};

function createBaseConfigRecord_Item(): ConfigRecord_Item {
  return { key: "", value: undefined };
}

export const ConfigRecord_Item: MessageFns<ConfigRecord_Item> = {
  encode(message: ConfigRecord_Item, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== undefined) {
      ConfigRecordValue.encode(message.value, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ConfigRecord_Item {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseConfigRecord_Item();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = ConfigRecordValue.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ConfigRecord_Item {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? ConfigRecordValue.fromJSON(object.value) : undefined,
    };
  },

  toJSON(message: ConfigRecord_Item): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== undefined) {
      obj.value = ConfigRecordValue.toJSON(message.value);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ConfigRecord_Item>, I>>(base?: I): ConfigRecord_Item {
    return ConfigRecord_Item.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ConfigRecord_Item>, I>>(object: I): ConfigRecord_Item {
    const message = createBaseConfigRecord_Item();
    message.key = object.key ?? "";
    message.value = (object.value !== undefined && object.value !== null)
      ? ConfigRecordValue.fromPartial(object.value)
      : undefined;
    return message;
  },
};

function createBaseRecordDict(): RecordDict {
  return { items: [] };
}

export const RecordDict: MessageFns<RecordDict> = {
  encode(message: RecordDict, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.items) {
      RecordDict_Item.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RecordDict {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRecordDict();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.items.push(RecordDict_Item.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RecordDict {
    return {
      items: globalThis.Array.isArray(object?.items) ? object.items.map((e: any) => RecordDict_Item.fromJSON(e)) : [],
    };
  },

  toJSON(message: RecordDict): unknown {
    const obj: any = {};
    if (message.items?.length) {
      obj.items = message.items.map((e) => RecordDict_Item.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RecordDict>, I>>(base?: I): RecordDict {
    return RecordDict.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RecordDict>, I>>(object: I): RecordDict {
    const message = createBaseRecordDict();
    message.items = object.items?.map((e) => RecordDict_Item.fromPartial(e)) || [];
    return message;
  },
};

function createBaseRecordDict_Item(): RecordDict_Item {
  return { key: "", arrayRecord: undefined, metricRecord: undefined, configRecord: undefined };
}

export const RecordDict_Item: MessageFns<RecordDict_Item> = {
  encode(message: RecordDict_Item, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.arrayRecord !== undefined) {
      ArrayRecord.encode(message.arrayRecord, writer.uint32(18).fork()).join();
    }
    if (message.metricRecord !== undefined) {
      MetricRecord.encode(message.metricRecord, writer.uint32(26).fork()).join();
    }
    if (message.configRecord !== undefined) {
      ConfigRecord.encode(message.configRecord, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RecordDict_Item {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRecordDict_Item();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.arrayRecord = ArrayRecord.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.metricRecord = MetricRecord.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.configRecord = ConfigRecord.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RecordDict_Item {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      arrayRecord: isSet(object.arrayRecord) ? ArrayRecord.fromJSON(object.arrayRecord) : undefined,
      metricRecord: isSet(object.metricRecord) ? MetricRecord.fromJSON(object.metricRecord) : undefined,
      configRecord: isSet(object.configRecord) ? ConfigRecord.fromJSON(object.configRecord) : undefined,
    };
  },

  toJSON(message: RecordDict_Item): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.arrayRecord !== undefined) {
      obj.arrayRecord = ArrayRecord.toJSON(message.arrayRecord);
    }
    if (message.metricRecord !== undefined) {
      obj.metricRecord = MetricRecord.toJSON(message.metricRecord);
    }
    if (message.configRecord !== undefined) {
      obj.configRecord = ConfigRecord.toJSON(message.configRecord);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RecordDict_Item>, I>>(base?: I): RecordDict_Item {
    return RecordDict_Item.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RecordDict_Item>, I>>(object: I): RecordDict_Item {
    const message = createBaseRecordDict_Item();
    message.key = object.key ?? "";
    message.arrayRecord = (object.arrayRecord !== undefined && object.arrayRecord !== null)
      ? ArrayRecord.fromPartial(object.arrayRecord)
      : undefined;
    message.metricRecord = (object.metricRecord !== undefined && object.metricRecord !== null)
      ? MetricRecord.fromPartial(object.metricRecord)
      : undefined;
    message.configRecord = (object.configRecord !== undefined && object.configRecord !== null)
      ? ConfigRecord.fromPartial(object.configRecord)
      : undefined;
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return globalThis.Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
