// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.10.1
//   protoc               v3.21.12
// source: flwr/proto/event.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Node } from "./node.js";

export const protobufPackage = "flwr.proto";

export enum EventType {
  /** ROUND_STARTED - Round lifecycle events */
  ROUND_STARTED = 0,
  ROUND_FIT_STARTED = 1,
  ROUND_FIT_AGGREGATED = 2,
  ROUND_FIT_FAILED = 3,
  ROUND_EVALUATE_STARTED = 4,
  ROUND_EVALUATE_AGGREGATED = 5,
  ROUND_EVALUATE_FAILED = 6,
  ROUND_COMPLETED = 7,
  ROUND_FAILED = 8,
  /** RUN_STARTED - Run lifecycle events */
  RUN_STARTED = 9,
  RUN_COMPLETED = 10,
  RUN_FAILED = 11,
  /** NODE_FIT_STARTED - Node task events */
  NODE_FIT_STARTED = 12,
  NODE_FIT_COMPLETED = 13,
  NODE_FIT_FAILED = 14,
  NODE_EVALUATE_STARTED = 15,
  NODE_EVALUATE_COMPLETED = 16,
  NODE_EVALUATE_FAILED = 17,
  /** NODE_CONNECTED - Node lifecycle events */
  NODE_CONNECTED = 18,
  NODE_DISCONNECTED = 19,
  UNRECOGNIZED = -1,
}

export function eventTypeFromJSON(object: any): EventType {
  switch (object) {
    case 0:
    case "ROUND_STARTED":
      return EventType.ROUND_STARTED;
    case 1:
    case "ROUND_FIT_STARTED":
      return EventType.ROUND_FIT_STARTED;
    case 2:
    case "ROUND_FIT_AGGREGATED":
      return EventType.ROUND_FIT_AGGREGATED;
    case 3:
    case "ROUND_FIT_FAILED":
      return EventType.ROUND_FIT_FAILED;
    case 4:
    case "ROUND_EVALUATE_STARTED":
      return EventType.ROUND_EVALUATE_STARTED;
    case 5:
    case "ROUND_EVALUATE_AGGREGATED":
      return EventType.ROUND_EVALUATE_AGGREGATED;
    case 6:
    case "ROUND_EVALUATE_FAILED":
      return EventType.ROUND_EVALUATE_FAILED;
    case 7:
    case "ROUND_COMPLETED":
      return EventType.ROUND_COMPLETED;
    case 8:
    case "ROUND_FAILED":
      return EventType.ROUND_FAILED;
    case 9:
    case "RUN_STARTED":
      return EventType.RUN_STARTED;
    case 10:
    case "RUN_COMPLETED":
      return EventType.RUN_COMPLETED;
    case 11:
    case "RUN_FAILED":
      return EventType.RUN_FAILED;
    case 12:
    case "NODE_FIT_STARTED":
      return EventType.NODE_FIT_STARTED;
    case 13:
    case "NODE_FIT_COMPLETED":
      return EventType.NODE_FIT_COMPLETED;
    case 14:
    case "NODE_FIT_FAILED":
      return EventType.NODE_FIT_FAILED;
    case 15:
    case "NODE_EVALUATE_STARTED":
      return EventType.NODE_EVALUATE_STARTED;
    case 16:
    case "NODE_EVALUATE_COMPLETED":
      return EventType.NODE_EVALUATE_COMPLETED;
    case 17:
    case "NODE_EVALUATE_FAILED":
      return EventType.NODE_EVALUATE_FAILED;
    case 18:
    case "NODE_CONNECTED":
      return EventType.NODE_CONNECTED;
    case 19:
    case "NODE_DISCONNECTED":
      return EventType.NODE_DISCONNECTED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return EventType.UNRECOGNIZED;
  }
}

export function eventTypeToJSON(object: EventType): string {
  switch (object) {
    case EventType.ROUND_STARTED:
      return "ROUND_STARTED";
    case EventType.ROUND_FIT_STARTED:
      return "ROUND_FIT_STARTED";
    case EventType.ROUND_FIT_AGGREGATED:
      return "ROUND_FIT_AGGREGATED";
    case EventType.ROUND_FIT_FAILED:
      return "ROUND_FIT_FAILED";
    case EventType.ROUND_EVALUATE_STARTED:
      return "ROUND_EVALUATE_STARTED";
    case EventType.ROUND_EVALUATE_AGGREGATED:
      return "ROUND_EVALUATE_AGGREGATED";
    case EventType.ROUND_EVALUATE_FAILED:
      return "ROUND_EVALUATE_FAILED";
    case EventType.ROUND_COMPLETED:
      return "ROUND_COMPLETED";
    case EventType.ROUND_FAILED:
      return "ROUND_FAILED";
    case EventType.RUN_STARTED:
      return "RUN_STARTED";
    case EventType.RUN_COMPLETED:
      return "RUN_COMPLETED";
    case EventType.RUN_FAILED:
      return "RUN_FAILED";
    case EventType.NODE_FIT_STARTED:
      return "NODE_FIT_STARTED";
    case EventType.NODE_FIT_COMPLETED:
      return "NODE_FIT_COMPLETED";
    case EventType.NODE_FIT_FAILED:
      return "NODE_FIT_FAILED";
    case EventType.NODE_EVALUATE_STARTED:
      return "NODE_EVALUATE_STARTED";
    case EventType.NODE_EVALUATE_COMPLETED:
      return "NODE_EVALUATE_COMPLETED";
    case EventType.NODE_EVALUATE_FAILED:
      return "NODE_EVALUATE_FAILED";
    case EventType.NODE_CONNECTED:
      return "NODE_CONNECTED";
    case EventType.NODE_DISCONNECTED:
      return "NODE_DISCONNECTED";
    case EventType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface Event {
  timestamp: number;
  runId?: string | undefined;
  nodeId: string;
  eventType: EventType;
  metadata: { [key: string]: string };
}

export interface Event_MetadataEntry {
  key: string;
  value: string;
}

export interface PushEventsRequest {
  node: Node | undefined;
  runId: string;
  events: Event[];
}

export interface PushEventsResponse {
}

function createBaseEvent(): Event {
  return { timestamp: 0, runId: undefined, nodeId: "0", eventType: 0, metadata: {} };
}

export const Event: MessageFns<Event> = {
  encode(message: Event, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.timestamp !== 0) {
      writer.uint32(9).double(message.timestamp);
    }
    if (message.runId !== undefined) {
      writer.uint32(16).uint64(message.runId);
    }
    if (message.nodeId !== "0") {
      writer.uint32(24).uint64(message.nodeId);
    }
    if (message.eventType !== 0) {
      writer.uint32(32).int32(message.eventType);
    }
    globalThis.Object.entries(message.metadata).forEach(([key, value]: [string, string]) => {
      Event_MetadataEntry.encode({ key: key as any, value }, writer.uint32(42).fork()).join();
    });
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Event {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 9) {
            break;
          }

          message.timestamp = reader.double();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.runId = reader.uint64().toString();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.nodeId = reader.uint64().toString();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.eventType = reader.int32() as any;
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          const entry5 = Event_MetadataEntry.decode(reader, reader.uint32());
          if (entry5.value !== undefined) {
            message.metadata[entry5.key] = entry5.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Event {
    return {
      timestamp: isSet(object.timestamp) ? globalThis.Number(object.timestamp) : 0,
      runId: isSet(object.runId) ? globalThis.String(object.runId) : undefined,
      nodeId: isSet(object.nodeId) ? globalThis.String(object.nodeId) : "0",
      eventType: isSet(object.eventType) ? eventTypeFromJSON(object.eventType) : 0,
      metadata: isObject(object.metadata)
        ? (globalThis.Object.entries(object.metadata) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : {},
    };
  },

  toJSON(message: Event): unknown {
    const obj: any = {};
    if (message.timestamp !== 0) {
      obj.timestamp = message.timestamp;
    }
    if (message.runId !== undefined) {
      obj.runId = message.runId;
    }
    if (message.nodeId !== "0") {
      obj.nodeId = message.nodeId;
    }
    if (message.eventType !== 0) {
      obj.eventType = eventTypeToJSON(message.eventType);
    }
    if (message.metadata) {
      const entries = globalThis.Object.entries(message.metadata) as [string, string][];
      if (entries.length > 0) {
        obj.metadata = {};
        entries.forEach(([k, v]) => {
          obj.metadata[k] = v;
        });
      }
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Event>, I>>(base?: I): Event {
    return Event.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Event>, I>>(object: I): Event {
    const message = createBaseEvent();
    message.timestamp = object.timestamp ?? 0;
    message.runId = object.runId ?? undefined;
    message.nodeId = object.nodeId ?? "0";
    message.eventType = object.eventType ?? 0;
    message.metadata = (globalThis.Object.entries(object.metadata ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    return message;
  },
};

function createBaseEvent_MetadataEntry(): Event_MetadataEntry {
  return { key: "", value: "" };
}

export const Event_MetadataEntry: MessageFns<Event_MetadataEntry> = {
  encode(message: Event_MetadataEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Event_MetadataEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEvent_MetadataEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Event_MetadataEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: Event_MetadataEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Event_MetadataEntry>, I>>(base?: I): Event_MetadataEntry {
    return Event_MetadataEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Event_MetadataEntry>, I>>(object: I): Event_MetadataEntry {
    const message = createBaseEvent_MetadataEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBasePushEventsRequest(): PushEventsRequest {
  return { node: undefined, runId: "0", events: [] };
}

export const PushEventsRequest: MessageFns<PushEventsRequest> = {
  encode(message: PushEventsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.node !== undefined) {
      Node.encode(message.node, writer.uint32(10).fork()).join();
    }
    if (message.runId !== "0") {
      writer.uint32(16).uint64(message.runId);
    }
    for (const v of message.events) {
      Event.encode(v!, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PushEventsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePushEventsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.node = Node.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.runId = reader.uint64().toString();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.events.push(Event.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PushEventsRequest {
    return {
      node: isSet(object.node) ? Node.fromJSON(object.node) : undefined,
      runId: isSet(object.runId) ? globalThis.String(object.runId) : "0",
      events: globalThis.Array.isArray(object?.events) ? object.events.map((e: any) => Event.fromJSON(e)) : [],
    };
  },

  toJSON(message: PushEventsRequest): unknown {
    const obj: any = {};
    if (message.node !== undefined) {
      obj.node = Node.toJSON(message.node);
    }
    if (message.runId !== "0") {
      obj.runId = message.runId;
    }
    if (message.events?.length) {
      obj.events = message.events.map((e) => Event.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PushEventsRequest>, I>>(base?: I): PushEventsRequest {
    return PushEventsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PushEventsRequest>, I>>(object: I): PushEventsRequest {
    const message = createBasePushEventsRequest();
    message.node = (object.node !== undefined && object.node !== null) ? Node.fromPartial(object.node) : undefined;
    message.runId = object.runId ?? "0";
    message.events = object.events?.map((e) => Event.fromPartial(e)) || [];
    return message;
  },
};

function createBasePushEventsResponse(): PushEventsResponse {
  return {};
}

export const PushEventsResponse: MessageFns<PushEventsResponse> = {
  encode(_: PushEventsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PushEventsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePushEventsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): PushEventsResponse {
    return {};
  },

  toJSON(_: PushEventsResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<PushEventsResponse>, I>>(base?: I): PushEventsResponse {
    return PushEventsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PushEventsResponse>, I>>(_: I): PushEventsResponse {
    const message = createBasePushEventsResponse();
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
