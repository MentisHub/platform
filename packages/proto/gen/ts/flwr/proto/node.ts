// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.10.1
//   protoc               v3.21.12
// source: flwr/proto/node.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "flwr.proto";

export interface Node {
  nodeId: string;
}

export interface NodeInfo {
  nodeId: string;
  ownerAid: string;
  ownerName: string;
  status: string;
  registeredAt: string;
  lastActivatedAt?: string | undefined;
  lastDeactivatedAt?: string | undefined;
  unregisteredAt?: string | undefined;
  onlineUntil?: number | undefined;
  heartbeatInterval: number;
  publicKey: Uint8Array;
}

function createBaseNode(): Node {
  return { nodeId: "0" };
}

export const Node: MessageFns<Node> = {
  encode(message: Node, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.nodeId !== "0") {
      writer.uint32(8).uint64(message.nodeId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Node {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseNode();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.nodeId = reader.uint64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Node {
    return { nodeId: isSet(object.nodeId) ? globalThis.String(object.nodeId) : "0" };
  },

  toJSON(message: Node): unknown {
    const obj: any = {};
    if (message.nodeId !== "0") {
      obj.nodeId = message.nodeId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Node>, I>>(base?: I): Node {
    return Node.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Node>, I>>(object: I): Node {
    const message = createBaseNode();
    message.nodeId = object.nodeId ?? "0";
    return message;
  },
};

function createBaseNodeInfo(): NodeInfo {
  return {
    nodeId: "0",
    ownerAid: "",
    ownerName: "",
    status: "",
    registeredAt: "",
    lastActivatedAt: undefined,
    lastDeactivatedAt: undefined,
    unregisteredAt: undefined,
    onlineUntil: undefined,
    heartbeatInterval: 0,
    publicKey: new Uint8Array(0),
  };
}

export const NodeInfo: MessageFns<NodeInfo> = {
  encode(message: NodeInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.nodeId !== "0") {
      writer.uint32(8).uint64(message.nodeId);
    }
    if (message.ownerAid !== "") {
      writer.uint32(18).string(message.ownerAid);
    }
    if (message.ownerName !== "") {
      writer.uint32(26).string(message.ownerName);
    }
    if (message.status !== "") {
      writer.uint32(34).string(message.status);
    }
    if (message.registeredAt !== "") {
      writer.uint32(42).string(message.registeredAt);
    }
    if (message.lastActivatedAt !== undefined) {
      writer.uint32(50).string(message.lastActivatedAt);
    }
    if (message.lastDeactivatedAt !== undefined) {
      writer.uint32(58).string(message.lastDeactivatedAt);
    }
    if (message.unregisteredAt !== undefined) {
      writer.uint32(66).string(message.unregisteredAt);
    }
    if (message.onlineUntil !== undefined) {
      writer.uint32(73).double(message.onlineUntil);
    }
    if (message.heartbeatInterval !== 0) {
      writer.uint32(81).double(message.heartbeatInterval);
    }
    if (message.publicKey.length !== 0) {
      writer.uint32(90).bytes(message.publicKey);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): NodeInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseNodeInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.nodeId = reader.uint64().toString();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.ownerAid = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.ownerName = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.status = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.registeredAt = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.lastActivatedAt = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.lastDeactivatedAt = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.unregisteredAt = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 73) {
            break;
          }

          message.onlineUntil = reader.double();
          continue;
        }
        case 10: {
          if (tag !== 81) {
            break;
          }

          message.heartbeatInterval = reader.double();
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.publicKey = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): NodeInfo {
    return {
      nodeId: isSet(object.nodeId) ? globalThis.String(object.nodeId) : "0",
      ownerAid: isSet(object.ownerAid) ? globalThis.String(object.ownerAid) : "",
      ownerName: isSet(object.ownerName) ? globalThis.String(object.ownerName) : "",
      status: isSet(object.status) ? globalThis.String(object.status) : "",
      registeredAt: isSet(object.registeredAt) ? globalThis.String(object.registeredAt) : "",
      lastActivatedAt: isSet(object.lastActivatedAt) ? globalThis.String(object.lastActivatedAt) : undefined,
      lastDeactivatedAt: isSet(object.lastDeactivatedAt) ? globalThis.String(object.lastDeactivatedAt) : undefined,
      unregisteredAt: isSet(object.unregisteredAt) ? globalThis.String(object.unregisteredAt) : undefined,
      onlineUntil: isSet(object.onlineUntil) ? globalThis.Number(object.onlineUntil) : undefined,
      heartbeatInterval: isSet(object.heartbeatInterval) ? globalThis.Number(object.heartbeatInterval) : 0,
      publicKey: isSet(object.publicKey) ? bytesFromBase64(object.publicKey) : new Uint8Array(0),
    };
  },

  toJSON(message: NodeInfo): unknown {
    const obj: any = {};
    if (message.nodeId !== "0") {
      obj.nodeId = message.nodeId;
    }
    if (message.ownerAid !== "") {
      obj.ownerAid = message.ownerAid;
    }
    if (message.ownerName !== "") {
      obj.ownerName = message.ownerName;
    }
    if (message.status !== "") {
      obj.status = message.status;
    }
    if (message.registeredAt !== "") {
      obj.registeredAt = message.registeredAt;
    }
    if (message.lastActivatedAt !== undefined) {
      obj.lastActivatedAt = message.lastActivatedAt;
    }
    if (message.lastDeactivatedAt !== undefined) {
      obj.lastDeactivatedAt = message.lastDeactivatedAt;
    }
    if (message.unregisteredAt !== undefined) {
      obj.unregisteredAt = message.unregisteredAt;
    }
    if (message.onlineUntil !== undefined) {
      obj.onlineUntil = message.onlineUntil;
    }
    if (message.heartbeatInterval !== 0) {
      obj.heartbeatInterval = message.heartbeatInterval;
    }
    if (message.publicKey.length !== 0) {
      obj.publicKey = base64FromBytes(message.publicKey);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<NodeInfo>, I>>(base?: I): NodeInfo {
    return NodeInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<NodeInfo>, I>>(object: I): NodeInfo {
    const message = createBaseNodeInfo();
    message.nodeId = object.nodeId ?? "0";
    message.ownerAid = object.ownerAid ?? "";
    message.ownerName = object.ownerName ?? "";
    message.status = object.status ?? "";
    message.registeredAt = object.registeredAt ?? "";
    message.lastActivatedAt = object.lastActivatedAt ?? undefined;
    message.lastDeactivatedAt = object.lastDeactivatedAt ?? undefined;
    message.unregisteredAt = object.unregisteredAt ?? undefined;
    message.onlineUntil = object.onlineUntil ?? undefined;
    message.heartbeatInterval = object.heartbeatInterval ?? 0;
    message.publicKey = object.publicKey ?? new Uint8Array(0);
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return globalThis.Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
