// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.10.1
//   protoc               v3.21.12
// source: flwr/proto/appio.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Fab } from "./fab.js";
import { Context, Message, ObjectTree } from "./message.js";
import { Run } from "./run.js";

export const protobufPackage = "flwr.proto";

/** ListAppsToLaunch messages */
export interface ListAppsToLaunchRequest {
}

export interface ListAppsToLaunchResponse {
  /** List of run IDs of the apps to launch */
  runIds: string[];
}

/** RequestToken messages */
export interface RequestTokenRequest {
  runId: string;
}

export interface RequestTokenResponse {
  token: string;
}

/** PushAppMessages messages */
export interface PushAppMessagesRequest {
  token: string;
  messagesList: Message[];
  runId: string;
  messageObjectTrees: ObjectTree[];
}

export interface PushAppMessagesResponse {
  messageIds: string[];
  objectsToPush: string[];
}

/** PullAppMessages messages */
export interface PullAppMessagesRequest {
  token: string;
  messageIds: string[];
  runId: string;
}

export interface PullAppMessagesResponse {
  messagesList: Message[];
  messageObjectTrees: ObjectTree[];
}

/** PullAppInputs messages */
export interface PullAppInputsRequest {
  token: string;
}

export interface PullAppInputsResponse {
  context: Context | undefined;
  run: Run | undefined;
  fab: Fab | undefined;
}

/** PushAppInputs messages */
export interface PushAppOutputsRequest {
  token: string;
  runId: string;
  context: Context | undefined;
}

export interface PushAppOutputsResponse {
}

function createBaseListAppsToLaunchRequest(): ListAppsToLaunchRequest {
  return {};
}

export const ListAppsToLaunchRequest: MessageFns<ListAppsToLaunchRequest> = {
  encode(_: ListAppsToLaunchRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListAppsToLaunchRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListAppsToLaunchRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): ListAppsToLaunchRequest {
    return {};
  },

  toJSON(_: ListAppsToLaunchRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<ListAppsToLaunchRequest>, I>>(base?: I): ListAppsToLaunchRequest {
    return ListAppsToLaunchRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListAppsToLaunchRequest>, I>>(_: I): ListAppsToLaunchRequest {
    const message = createBaseListAppsToLaunchRequest();
    return message;
  },
};

function createBaseListAppsToLaunchResponse(): ListAppsToLaunchResponse {
  return { runIds: [] };
}

export const ListAppsToLaunchResponse: MessageFns<ListAppsToLaunchResponse> = {
  encode(message: ListAppsToLaunchResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    writer.uint32(10).fork();
    for (const v of message.runIds) {
      writer.uint64(v);
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListAppsToLaunchResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListAppsToLaunchResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag === 8) {
            message.runIds.push(reader.uint64().toString());

            continue;
          }

          if (tag === 10) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.runIds.push(reader.uint64().toString());
            }

            continue;
          }

          break;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListAppsToLaunchResponse {
    return {
      runIds: globalThis.Array.isArray(object?.runIds) ? object.runIds.map((e: any) => globalThis.String(e)) : [],
    };
  },

  toJSON(message: ListAppsToLaunchResponse): unknown {
    const obj: any = {};
    if (message.runIds?.length) {
      obj.runIds = message.runIds;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListAppsToLaunchResponse>, I>>(base?: I): ListAppsToLaunchResponse {
    return ListAppsToLaunchResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListAppsToLaunchResponse>, I>>(object: I): ListAppsToLaunchResponse {
    const message = createBaseListAppsToLaunchResponse();
    message.runIds = object.runIds?.map((e) => e) || [];
    return message;
  },
};

function createBaseRequestTokenRequest(): RequestTokenRequest {
  return { runId: "0" };
}

export const RequestTokenRequest: MessageFns<RequestTokenRequest> = {
  encode(message: RequestTokenRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.runId !== "0") {
      writer.uint32(8).uint64(message.runId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RequestTokenRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRequestTokenRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.runId = reader.uint64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RequestTokenRequest {
    return { runId: isSet(object.runId) ? globalThis.String(object.runId) : "0" };
  },

  toJSON(message: RequestTokenRequest): unknown {
    const obj: any = {};
    if (message.runId !== "0") {
      obj.runId = message.runId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RequestTokenRequest>, I>>(base?: I): RequestTokenRequest {
    return RequestTokenRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RequestTokenRequest>, I>>(object: I): RequestTokenRequest {
    const message = createBaseRequestTokenRequest();
    message.runId = object.runId ?? "0";
    return message;
  },
};

function createBaseRequestTokenResponse(): RequestTokenResponse {
  return { token: "" };
}

export const RequestTokenResponse: MessageFns<RequestTokenResponse> = {
  encode(message: RequestTokenResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.token !== "") {
      writer.uint32(10).string(message.token);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RequestTokenResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRequestTokenResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.token = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RequestTokenResponse {
    return { token: isSet(object.token) ? globalThis.String(object.token) : "" };
  },

  toJSON(message: RequestTokenResponse): unknown {
    const obj: any = {};
    if (message.token !== "") {
      obj.token = message.token;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RequestTokenResponse>, I>>(base?: I): RequestTokenResponse {
    return RequestTokenResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RequestTokenResponse>, I>>(object: I): RequestTokenResponse {
    const message = createBaseRequestTokenResponse();
    message.token = object.token ?? "";
    return message;
  },
};

function createBasePushAppMessagesRequest(): PushAppMessagesRequest {
  return { token: "", messagesList: [], runId: "0", messageObjectTrees: [] };
}

export const PushAppMessagesRequest: MessageFns<PushAppMessagesRequest> = {
  encode(message: PushAppMessagesRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.token !== "") {
      writer.uint32(10).string(message.token);
    }
    for (const v of message.messagesList) {
      Message.encode(v!, writer.uint32(18).fork()).join();
    }
    if (message.runId !== "0") {
      writer.uint32(24).uint64(message.runId);
    }
    for (const v of message.messageObjectTrees) {
      ObjectTree.encode(v!, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PushAppMessagesRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePushAppMessagesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.token = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.messagesList.push(Message.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.runId = reader.uint64().toString();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.messageObjectTrees.push(ObjectTree.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PushAppMessagesRequest {
    return {
      token: isSet(object.token) ? globalThis.String(object.token) : "",
      messagesList: globalThis.Array.isArray(object?.messagesList)
        ? object.messagesList.map((e: any) => Message.fromJSON(e))
        : [],
      runId: isSet(object.runId) ? globalThis.String(object.runId) : "0",
      messageObjectTrees: globalThis.Array.isArray(object?.messageObjectTrees)
        ? object.messageObjectTrees.map((e: any) => ObjectTree.fromJSON(e))
        : [],
    };
  },

  toJSON(message: PushAppMessagesRequest): unknown {
    const obj: any = {};
    if (message.token !== "") {
      obj.token = message.token;
    }
    if (message.messagesList?.length) {
      obj.messagesList = message.messagesList.map((e) => Message.toJSON(e));
    }
    if (message.runId !== "0") {
      obj.runId = message.runId;
    }
    if (message.messageObjectTrees?.length) {
      obj.messageObjectTrees = message.messageObjectTrees.map((e) => ObjectTree.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PushAppMessagesRequest>, I>>(base?: I): PushAppMessagesRequest {
    return PushAppMessagesRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PushAppMessagesRequest>, I>>(object: I): PushAppMessagesRequest {
    const message = createBasePushAppMessagesRequest();
    message.token = object.token ?? "";
    message.messagesList = object.messagesList?.map((e) => Message.fromPartial(e)) || [];
    message.runId = object.runId ?? "0";
    message.messageObjectTrees = object.messageObjectTrees?.map((e) => ObjectTree.fromPartial(e)) || [];
    return message;
  },
};

function createBasePushAppMessagesResponse(): PushAppMessagesResponse {
  return { messageIds: [], objectsToPush: [] };
}

export const PushAppMessagesResponse: MessageFns<PushAppMessagesResponse> = {
  encode(message: PushAppMessagesResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.messageIds) {
      writer.uint32(10).string(v!);
    }
    for (const v of message.objectsToPush) {
      writer.uint32(18).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PushAppMessagesResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePushAppMessagesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.messageIds.push(reader.string());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.objectsToPush.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PushAppMessagesResponse {
    return {
      messageIds: globalThis.Array.isArray(object?.messageIds)
        ? object.messageIds.map((e: any) => globalThis.String(e))
        : [],
      objectsToPush: globalThis.Array.isArray(object?.objectsToPush)
        ? object.objectsToPush.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: PushAppMessagesResponse): unknown {
    const obj: any = {};
    if (message.messageIds?.length) {
      obj.messageIds = message.messageIds;
    }
    if (message.objectsToPush?.length) {
      obj.objectsToPush = message.objectsToPush;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PushAppMessagesResponse>, I>>(base?: I): PushAppMessagesResponse {
    return PushAppMessagesResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PushAppMessagesResponse>, I>>(object: I): PushAppMessagesResponse {
    const message = createBasePushAppMessagesResponse();
    message.messageIds = object.messageIds?.map((e) => e) || [];
    message.objectsToPush = object.objectsToPush?.map((e) => e) || [];
    return message;
  },
};

function createBasePullAppMessagesRequest(): PullAppMessagesRequest {
  return { token: "", messageIds: [], runId: "0" };
}

export const PullAppMessagesRequest: MessageFns<PullAppMessagesRequest> = {
  encode(message: PullAppMessagesRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.token !== "") {
      writer.uint32(10).string(message.token);
    }
    for (const v of message.messageIds) {
      writer.uint32(18).string(v!);
    }
    if (message.runId !== "0") {
      writer.uint32(24).uint64(message.runId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PullAppMessagesRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePullAppMessagesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.token = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.messageIds.push(reader.string());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.runId = reader.uint64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PullAppMessagesRequest {
    return {
      token: isSet(object.token) ? globalThis.String(object.token) : "",
      messageIds: globalThis.Array.isArray(object?.messageIds)
        ? object.messageIds.map((e: any) => globalThis.String(e))
        : [],
      runId: isSet(object.runId) ? globalThis.String(object.runId) : "0",
    };
  },

  toJSON(message: PullAppMessagesRequest): unknown {
    const obj: any = {};
    if (message.token !== "") {
      obj.token = message.token;
    }
    if (message.messageIds?.length) {
      obj.messageIds = message.messageIds;
    }
    if (message.runId !== "0") {
      obj.runId = message.runId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PullAppMessagesRequest>, I>>(base?: I): PullAppMessagesRequest {
    return PullAppMessagesRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PullAppMessagesRequest>, I>>(object: I): PullAppMessagesRequest {
    const message = createBasePullAppMessagesRequest();
    message.token = object.token ?? "";
    message.messageIds = object.messageIds?.map((e) => e) || [];
    message.runId = object.runId ?? "0";
    return message;
  },
};

function createBasePullAppMessagesResponse(): PullAppMessagesResponse {
  return { messagesList: [], messageObjectTrees: [] };
}

export const PullAppMessagesResponse: MessageFns<PullAppMessagesResponse> = {
  encode(message: PullAppMessagesResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.messagesList) {
      Message.encode(v!, writer.uint32(10).fork()).join();
    }
    for (const v of message.messageObjectTrees) {
      ObjectTree.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PullAppMessagesResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePullAppMessagesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.messagesList.push(Message.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.messageObjectTrees.push(ObjectTree.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PullAppMessagesResponse {
    return {
      messagesList: globalThis.Array.isArray(object?.messagesList)
        ? object.messagesList.map((e: any) => Message.fromJSON(e))
        : [],
      messageObjectTrees: globalThis.Array.isArray(object?.messageObjectTrees)
        ? object.messageObjectTrees.map((e: any) => ObjectTree.fromJSON(e))
        : [],
    };
  },

  toJSON(message: PullAppMessagesResponse): unknown {
    const obj: any = {};
    if (message.messagesList?.length) {
      obj.messagesList = message.messagesList.map((e) => Message.toJSON(e));
    }
    if (message.messageObjectTrees?.length) {
      obj.messageObjectTrees = message.messageObjectTrees.map((e) => ObjectTree.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PullAppMessagesResponse>, I>>(base?: I): PullAppMessagesResponse {
    return PullAppMessagesResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PullAppMessagesResponse>, I>>(object: I): PullAppMessagesResponse {
    const message = createBasePullAppMessagesResponse();
    message.messagesList = object.messagesList?.map((e) => Message.fromPartial(e)) || [];
    message.messageObjectTrees = object.messageObjectTrees?.map((e) => ObjectTree.fromPartial(e)) || [];
    return message;
  },
};

function createBasePullAppInputsRequest(): PullAppInputsRequest {
  return { token: "" };
}

export const PullAppInputsRequest: MessageFns<PullAppInputsRequest> = {
  encode(message: PullAppInputsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.token !== "") {
      writer.uint32(10).string(message.token);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PullAppInputsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePullAppInputsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.token = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PullAppInputsRequest {
    return { token: isSet(object.token) ? globalThis.String(object.token) : "" };
  },

  toJSON(message: PullAppInputsRequest): unknown {
    const obj: any = {};
    if (message.token !== "") {
      obj.token = message.token;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PullAppInputsRequest>, I>>(base?: I): PullAppInputsRequest {
    return PullAppInputsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PullAppInputsRequest>, I>>(object: I): PullAppInputsRequest {
    const message = createBasePullAppInputsRequest();
    message.token = object.token ?? "";
    return message;
  },
};

function createBasePullAppInputsResponse(): PullAppInputsResponse {
  return { context: undefined, run: undefined, fab: undefined };
}

export const PullAppInputsResponse: MessageFns<PullAppInputsResponse> = {
  encode(message: PullAppInputsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.context !== undefined) {
      Context.encode(message.context, writer.uint32(10).fork()).join();
    }
    if (message.run !== undefined) {
      Run.encode(message.run, writer.uint32(18).fork()).join();
    }
    if (message.fab !== undefined) {
      Fab.encode(message.fab, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PullAppInputsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePullAppInputsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.context = Context.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.run = Run.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.fab = Fab.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PullAppInputsResponse {
    return {
      context: isSet(object.context) ? Context.fromJSON(object.context) : undefined,
      run: isSet(object.run) ? Run.fromJSON(object.run) : undefined,
      fab: isSet(object.fab) ? Fab.fromJSON(object.fab) : undefined,
    };
  },

  toJSON(message: PullAppInputsResponse): unknown {
    const obj: any = {};
    if (message.context !== undefined) {
      obj.context = Context.toJSON(message.context);
    }
    if (message.run !== undefined) {
      obj.run = Run.toJSON(message.run);
    }
    if (message.fab !== undefined) {
      obj.fab = Fab.toJSON(message.fab);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PullAppInputsResponse>, I>>(base?: I): PullAppInputsResponse {
    return PullAppInputsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PullAppInputsResponse>, I>>(object: I): PullAppInputsResponse {
    const message = createBasePullAppInputsResponse();
    message.context = (object.context !== undefined && object.context !== null)
      ? Context.fromPartial(object.context)
      : undefined;
    message.run = (object.run !== undefined && object.run !== null) ? Run.fromPartial(object.run) : undefined;
    message.fab = (object.fab !== undefined && object.fab !== null) ? Fab.fromPartial(object.fab) : undefined;
    return message;
  },
};

function createBasePushAppOutputsRequest(): PushAppOutputsRequest {
  return { token: "", runId: "0", context: undefined };
}

export const PushAppOutputsRequest: MessageFns<PushAppOutputsRequest> = {
  encode(message: PushAppOutputsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.token !== "") {
      writer.uint32(10).string(message.token);
    }
    if (message.runId !== "0") {
      writer.uint32(16).uint64(message.runId);
    }
    if (message.context !== undefined) {
      Context.encode(message.context, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PushAppOutputsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePushAppOutputsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.token = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.runId = reader.uint64().toString();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.context = Context.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PushAppOutputsRequest {
    return {
      token: isSet(object.token) ? globalThis.String(object.token) : "",
      runId: isSet(object.runId) ? globalThis.String(object.runId) : "0",
      context: isSet(object.context) ? Context.fromJSON(object.context) : undefined,
    };
  },

  toJSON(message: PushAppOutputsRequest): unknown {
    const obj: any = {};
    if (message.token !== "") {
      obj.token = message.token;
    }
    if (message.runId !== "0") {
      obj.runId = message.runId;
    }
    if (message.context !== undefined) {
      obj.context = Context.toJSON(message.context);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PushAppOutputsRequest>, I>>(base?: I): PushAppOutputsRequest {
    return PushAppOutputsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PushAppOutputsRequest>, I>>(object: I): PushAppOutputsRequest {
    const message = createBasePushAppOutputsRequest();
    message.token = object.token ?? "";
    message.runId = object.runId ?? "0";
    message.context = (object.context !== undefined && object.context !== null)
      ? Context.fromPartial(object.context)
      : undefined;
    return message;
  },
};

function createBasePushAppOutputsResponse(): PushAppOutputsResponse {
  return {};
}

export const PushAppOutputsResponse: MessageFns<PushAppOutputsResponse> = {
  encode(_: PushAppOutputsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PushAppOutputsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePushAppOutputsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): PushAppOutputsResponse {
    return {};
  },

  toJSON(_: PushAppOutputsResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<PushAppOutputsResponse>, I>>(base?: I): PushAppOutputsResponse {
    return PushAppOutputsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PushAppOutputsResponse>, I>>(_: I): PushAppOutputsResponse {
    const message = createBasePushAppOutputsResponse();
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
