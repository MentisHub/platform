// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.10.1
//   protoc               v3.21.12
// source: flwr/proto/fab.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Node } from "./node.js";

export const protobufPackage = "flwr.proto";

export interface Fab {
  /**
   * This field is the hash of the data field. It is used to identify the data.
   * The hash is calculated using the SHA-256 algorithm and is represented as a
   * hex string (sha256hex).
   */
  hashStr: string;
  /** This field contains the fab file contents a one bytes blob. */
  content: Uint8Array;
  /** Verifications. */
  verifications: { [key: string]: string };
}

export interface Fab_VerificationsEntry {
  key: string;
  value: string;
}

export interface GetFabRequest {
  node: Node | undefined;
  hashStr: string;
  runId: string;
}

export interface GetFabResponse {
  fab: Fab | undefined;
}

function createBaseFab(): Fab {
  return { hashStr: "", content: new Uint8Array(0), verifications: {} };
}

export const Fab: MessageFns<Fab> = {
  encode(message: Fab, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.hashStr !== "") {
      writer.uint32(10).string(message.hashStr);
    }
    if (message.content.length !== 0) {
      writer.uint32(18).bytes(message.content);
    }
    globalThis.Object.entries(message.verifications).forEach(([key, value]: [string, string]) => {
      Fab_VerificationsEntry.encode({ key: key as any, value }, writer.uint32(26).fork()).join();
    });
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Fab {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFab();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.hashStr = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.content = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          const entry3 = Fab_VerificationsEntry.decode(reader, reader.uint32());
          if (entry3.value !== undefined) {
            message.verifications[entry3.key] = entry3.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Fab {
    return {
      hashStr: isSet(object.hashStr) ? globalThis.String(object.hashStr) : "",
      content: isSet(object.content) ? bytesFromBase64(object.content) : new Uint8Array(0),
      verifications: isObject(object.verifications)
        ? (globalThis.Object.entries(object.verifications) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : {},
    };
  },

  toJSON(message: Fab): unknown {
    const obj: any = {};
    if (message.hashStr !== "") {
      obj.hashStr = message.hashStr;
    }
    if (message.content.length !== 0) {
      obj.content = base64FromBytes(message.content);
    }
    if (message.verifications) {
      const entries = globalThis.Object.entries(message.verifications) as [string, string][];
      if (entries.length > 0) {
        obj.verifications = {};
        entries.forEach(([k, v]) => {
          obj.verifications[k] = v;
        });
      }
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Fab>, I>>(base?: I): Fab {
    return Fab.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Fab>, I>>(object: I): Fab {
    const message = createBaseFab();
    message.hashStr = object.hashStr ?? "";
    message.content = object.content ?? new Uint8Array(0);
    message.verifications = (globalThis.Object.entries(object.verifications ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    return message;
  },
};

function createBaseFab_VerificationsEntry(): Fab_VerificationsEntry {
  return { key: "", value: "" };
}

export const Fab_VerificationsEntry: MessageFns<Fab_VerificationsEntry> = {
  encode(message: Fab_VerificationsEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Fab_VerificationsEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFab_VerificationsEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Fab_VerificationsEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: Fab_VerificationsEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Fab_VerificationsEntry>, I>>(base?: I): Fab_VerificationsEntry {
    return Fab_VerificationsEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Fab_VerificationsEntry>, I>>(object: I): Fab_VerificationsEntry {
    const message = createBaseFab_VerificationsEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseGetFabRequest(): GetFabRequest {
  return { node: undefined, hashStr: "", runId: "0" };
}

export const GetFabRequest: MessageFns<GetFabRequest> = {
  encode(message: GetFabRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.node !== undefined) {
      Node.encode(message.node, writer.uint32(10).fork()).join();
    }
    if (message.hashStr !== "") {
      writer.uint32(18).string(message.hashStr);
    }
    if (message.runId !== "0") {
      writer.uint32(24).uint64(message.runId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetFabRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetFabRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.node = Node.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.hashStr = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.runId = reader.uint64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetFabRequest {
    return {
      node: isSet(object.node) ? Node.fromJSON(object.node) : undefined,
      hashStr: isSet(object.hashStr) ? globalThis.String(object.hashStr) : "",
      runId: isSet(object.runId) ? globalThis.String(object.runId) : "0",
    };
  },

  toJSON(message: GetFabRequest): unknown {
    const obj: any = {};
    if (message.node !== undefined) {
      obj.node = Node.toJSON(message.node);
    }
    if (message.hashStr !== "") {
      obj.hashStr = message.hashStr;
    }
    if (message.runId !== "0") {
      obj.runId = message.runId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetFabRequest>, I>>(base?: I): GetFabRequest {
    return GetFabRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetFabRequest>, I>>(object: I): GetFabRequest {
    const message = createBaseGetFabRequest();
    message.node = (object.node !== undefined && object.node !== null) ? Node.fromPartial(object.node) : undefined;
    message.hashStr = object.hashStr ?? "";
    message.runId = object.runId ?? "0";
    return message;
  },
};

function createBaseGetFabResponse(): GetFabResponse {
  return { fab: undefined };
}

export const GetFabResponse: MessageFns<GetFabResponse> = {
  encode(message: GetFabResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.fab !== undefined) {
      Fab.encode(message.fab, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetFabResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetFabResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.fab = Fab.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetFabResponse {
    return { fab: isSet(object.fab) ? Fab.fromJSON(object.fab) : undefined };
  },

  toJSON(message: GetFabResponse): unknown {
    const obj: any = {};
    if (message.fab !== undefined) {
      obj.fab = Fab.toJSON(message.fab);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetFabResponse>, I>>(base?: I): GetFabResponse {
    return GetFabResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetFabResponse>, I>>(object: I): GetFabResponse {
    const message = createBaseGetFabResponse();
    message.fab = (object.fab !== undefined && object.fab !== null) ? Fab.fromPartial(object.fab) : undefined;
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return globalThis.Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
